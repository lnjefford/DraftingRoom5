# DR5-076 — Integrated Overview, Assets, and Library

Executor: `01a09848-5d39-7670-8013-394782400976`. Push **NO**. Accumulated Phase 9 work and released defaults **0.26.1 / 26003** remain unchanged.

## Delivered behavior

Overview now opens with the financial map. Its tracked total and visible treatment rows use the same pure integer-cent aggregation as Assets. The next card shows retirement age and the existing forecast engine's modeled success state, then routes directly to Forecast; there is no separate runway. The compact data-health card selects the affected linked account, property, or Epic import and routes directly to its repair/detail flow. Last-good values remain visible when a source needs attention or is stale.

Assets shows the tracked total, the exact amount included in Forecast, and rows for pre-tax, Roth, taxable, tax-free, property equity, and Epic stock. The table routes to the existing grouped Accounts destination. It counts only current balances for active manual/linked accounts, the active workbook's vested Epic value less loans, and owned property equity. Property and Epic wrapper accounts carry no second balance and are never counted again.

The book icon opens the Library with four bundled official government resources, visibly reviewed September 21, 2026. Social Security, Medicare, IRS, and Investor.gov links were opened and verified during implementation. The only mutable library data is `ChecklistState(catalogId, checked, updatedAt)` through the existing repository. No file chooser, URI, document bytes, source filename, or private document model exists in this destination.

Loading, empty, current, stale/attention, and repository-error states state what is known and preserve the last accepted data. The screens keep the approved evergreen theme, semantic headings, merged label/value descriptions, at least 48 dp actions, document-order focus, ordinary route restoration, and toolbar/system Back behavior. The removed Strategy, runway, rental, Properties overview, Data sources, and general Retirement settings destinations remain absent.

## Exact reconciliation evidence

The focused fixture reconciles **$78,550.00 tracked** as:

- Roth manual account: **$1,000.00**
- Pre-tax linked account: **$2,000.00**
- active workbook Epic vested value less loans: **$550.00**
- 50%-owned property equity after mortgage: **$75,000.00**

The linked account is excluded from Forecast in that fixture, so the Forecast-eligible amount is exactly **$76,550.00**. The property and Epic wrappers contribute zero additional balance. Tests require the six displayed treatment rows to sum exactly to the tracked total.

## Accessibility and visual review

Twenty-eight new references cover Overview ready, affected-account attention, empty, loading, error, Assets, and Library across compact, tall, landscape, and 2× text. The five existing Retirement shell states were refreshed across the same four form factors. Manual inspection found a large-text collision between the property-equity label and value; treatment rows now stack at 1.5× text and above. Final inspection confirmed readable light-on-evergreen contrast, financial-map-first order, scoped attention, scroll-safe long content, and visible Library review/checklist content.

## Verification

- **391 JVM tests** passed with zero failures, errors, or skips. Focused tests cover exact aggregation/no double counting, account/property/import/stale routing, official Library metadata, and integrated restoration/back behavior. Existing repository tests verify checklist-only round trips.
- **443 screenshot comparisons** passed with zero failures, errors, or skips. This is 28 new integrated-state references on top of the prior 415.
- `lintDebug` passed with **zero errors**, 31 warnings, and four informational findings. `assembleDebug` and `assembleDebugAndroidTest` passed.
- **105 reference assertions**, 14 full-path forecast replays, 112 tax/SS grid rows, and 14 architecture contract tests passed with network/private-data guards.
- The application APK remains **0.26.1 / 26003**. Test credential canaries, manual/forecast parity fixture names, raw workbook files, and the full-path fixture canary are absent. The `.xlsm` text that remains is only the user-facing file-picker guidance, not workbook content.
- `git diff --check`, new-source trailing-whitespace checks, and conflict-marker checks pass.

The default OneDrive build output reproduced its documented Windows file-lock failures. Final compilation and validation used the isolated DR5-076 build directory; the init script relocates outputs and skips only the two no-source generated-Java tasks after retaining the generated debug `BuildConfig` class. Kotlin compilation, JVM tests, lint, dexing, both APKs, screenshot rendering, and screenshot comparison all ran.

No real accounts, provider credentials, workbook, address, or private financial data was used. No commit, push, tag, release, or version change occurred. DR5-077 is dependency-cleared.
